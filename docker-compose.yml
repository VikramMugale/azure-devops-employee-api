# Local development – API container talks to NeonDB via DATABASE_URL
services:
  api:
    build:
      context: .
      dockerfile: Dockerfile
    image: employee-api:local
    ports:
      - "8000:8000"
    env_file:
      - .env
    environment:
      APP_ENV: development
      PORT: "8000"
    healthcheck:
      test: ["CMD", "python", "-c", "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health')"]
      interval: 15s
      timeout: 5s
      retries: 3
      start_period: 10s
    restart: unless-stopped
